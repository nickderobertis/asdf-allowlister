# Optional contributor convenience for direnv users. Running this plugin with
# asdf does not require direnv; this only improves the local dev loop.
#
# Loads a local, git-ignored .env (for example GITHUB_API_TOKEN) when present,
# and re-evaluates when the pinned tool versions change. No secrets belong here.
dotenv_if_exists .env
watch_file .tool-versions
