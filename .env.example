# Copy to .env (git-ignored) and fill in if the anonymous GitHub API rate limit
# interrupts version listing. Any token with public read access is enough. The
# token only raises the API rate limit; it is never sent with binary downloads.
# Never commit a real token.
GITHUB_API_TOKEN=
